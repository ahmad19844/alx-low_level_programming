#include "main.h"

/**
 * main - check the code
 * @s: strint to be reversed
 * Return: Always 0.
 */
void print_rev(char *s)
{
	    char *str;
		    str = "In few months to come, i will beome software engi";
		    print_rev(str);
		        return (0);
}
